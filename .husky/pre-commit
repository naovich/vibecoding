echo "🔍 Running pre-commit checks..."

# 1. lint-staged (lint + format staged files)
npx lint-staged

# 2. Check TypeScript types (fast, no emit)
echo "📘 Checking TypeScript types..."
npm run type-check

# 3. Generate documentation (LAST - after all formatting/linting)
echo "📚 Generating documentation..."
npm run docs
git add FILE_TREE.md CODEBASE.md

# Note: The following are now enforced by ESLint:
# - no-explicit-any rule (replaces check-no-any.js)
# - max-lines rule (replaces check-file-size.js)
# - Tailwind CSS rules (replaces check-tailwind-syntax.js)
# All checked automatically during lint-staged

echo "✅ Pre-commit checks passed!"
